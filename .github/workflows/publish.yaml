name: Build & publish images to the package registries for releases

on:
  release:
    types: [published]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build_element_call:
    if: ${{ github.event_name == 'release' }}
    uses: ./.github/workflows/build-element-call.yaml
    with:
      vite_app_version: ${{ github.event.release.tag_name }}
  publish_tarball:
    needs: build_element_call
    if: always()
    name: Publish tarball
    runs-on: ubuntu-latest
    permissions:
      contents: write # required to upload release asset
      packages: write
    steps:
      - name: 📥 Download built element-call artifact
        uses: actions/download-artifact@fa0a91b85d4f404e444e00e005971372dc801d16 # v4
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: ${{ github.event.workflow_run.id || github.run_id }}
          name: build-output
          path: dist
      - name: Create Tarball
        env:
          TARBALL_VERSION: ${{ github.event.release.tag_name }}
        run: |
          tar --numeric-owner --transform "s/dist/element-call-${TARBALL_VERSION}/" -cvzf element-call-${TARBALL_VERSION}.tar.gz dist
          sha256sum element-call-${TARBALL_VERSION}.tar.gz > element-call-${TARBALL_VERSION}.tar.gz.sha256
      - name: Upload
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            element-call-${{ github.event.release.tag_name }}.tar.gz
            element-call-${{ github.event.release.tag_name }}.tar.gz.sha256
  publish_docker:
    needs: build_element_call
    if: always()
    name: Publish docker
    permissions:
      contents: write
      packages: write
    uses: ./.github/workflows/build-and-publish-docker.yaml
    with:
      artifact_run_id: ${{ github.event.workflow_run.id || github.run_id }}
      docker_tags: |
        type=sha,format=short,event=branch
        type=semver,pattern=v{{version}}
  publish_npm:
    needs: build_element_call
    if: always()
    name: Publish NPM
    runs-on: ubuntu-latest
    permissions:
      contents: write # required to upload release asset
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: 📥 Download built element-call artifact
        uses: actions/download-artifact@fa0a91b85d4f404e444e00e005971372dc801d16 # v4        
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: ${{ github.event.workflow_run.id || github.run_id }}
          name: build-output
          path: embedded/web/dist

      - name: Setup node
        uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          registry-url: 'https://npm.pkg.github.com'
          scope: '@element-call'
      - name: Publish npm
        working-directory: embedded/web
        run: |
          npm version ${{ github.event.release.tag_name }} --no-git-tag-version
          npm publish --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Add release note
        uses: softprops/action-gh-release@v2
        with:
          append_body: |
            ## NPM
            - [@element-call/element-call-embedded](https://npm.pkg.github.com/package/@element-call/element-call-embedded/v/${{ github.event.release.tag_name }})
